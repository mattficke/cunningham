<div>
  <% @feeds.each do |feed| %>
  <div>
    <h2><%= feed.name %></h2>
    <% Instagram.location_recent_media(feed.location).first(3).each do |photo| %>
      <span><%= image_tag photo.images.low_resolution.url %></span>
    <% end %>
  </div>
  <% end %>
</div>
